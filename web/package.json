{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "cargo test && wasm-pack test --headless --chrome",
    "build": "tsc && vite build",
    "release": "tsc && vite build --base=./",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "5.7",
    "vite": "6.0"
  }
}